\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mystyle}[2021/02/09 Selfmade mystyle package] %prefix som visstnok trengs for at LaTeX skal skjønne .sty-dokumentet
\usepackage[utf8]{inputenc} % Løser problem med å skrive andre enn engelske bokstaver f.eks æ,ø,å.- Unicode Transformation Format
\usepackage[T1]{fontenc,url} %Støtter koding av forskjellige fonter. |  Gjør internett- og e-mail adresser klikkbare i tex-dokumentet.
\usepackage{babel} %språkpakke: trengst for alt utanom US English | tekstkompilering
\usepackage{varioref} %referansepakke - see fig X on page Y/ next/previous page
%\usepackage{showkeys} %easily keep track of the labels you add or use (e.g. see them in pdf)          !!!Remove before final print!!!
\usepackage{textcomp} %- Støtter bruk av forskjellige fonter som dollartegn, copyright, en kvart,
%en halv mm, se http://gcp.fcaglp.unlp.edu.ar/_media/integrantes:psantamaria:latex:textcomp.pdf
% \usepackage{multirow} %for tables with multirow sections

\usepackage[a4paper,width=150mm,top=25mm,bottom=25mm]{geometry}
\usepackage{subcaption} % caption under subfigures?
%\usepackage{wrapfig} % wrap text around figures

%biblatex example setup
%\usepackage[
%style=authoryear-comp,
%sorting=nyt, %sort intext by name year then title
%maxcitenames=2, %max intext authors
%minnames=1, %seemed to sort the lacking a,b,c after Meek14a,Meek14b
%maxbibnames=30, %max endtext authors
%url=false, %Dont print URL
%sortcites=true, %sorts intext citations
%firstinits=true %First name in initial form! Finally found it :D
%backend=biber
%]{biblatex}
%[authordate,backend=biber,maxnames=1,]maxcitenames=1,mincitenames=1,maxbibnames=30,
\usepackage[style=authoryear-comp,maxbibnames=30,maxcitenames=2,uniquelist=false,firstinits=true,url=false,isbn=false,backend=biber]{biblatex}
\urlstyle{sf} %change url to sans serif for better linebreaking
%\usepackage{hyperref} % to get clickable links

\usepackage[subtle]{savetrees} %tries to use less pages (subtle, moderate, extreme)

\usepackage[style=english]{csquotes} %provides international handling of quote marks and should always be included when you use the biblatex package.

\usepackage{gensymb} %Provides generic commands \degree, \celsius, \perthousand, \micro and \ohm which work both in text and maths mode


%https://no.overleaf.com/learn/latex/How_to_Write_a_Thesis_in_LaTeX_(Part_2):_Page_Layout
\usepackage{fancyhdr}
\usepackage{extramarks}
\renewcommand{\chaptermark}[1]{%
	\markboth{#1}{}}
\pagestyle{fancy}
\fancyhead{}
\fancyhead[RO,LE]{Flashing Large Mammals}
\fancyfoot{} % remove pagenumber
\fancyfoot[RO,LE]{\thepage} %pagenumber left and right
\fancyfoot[CO,CE]{\leftmark}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}


\usepackage{titlesec} % to adjust chapter formatting

\titleformat{\chapter}[block]
{\normalfont\huge\bfseries}{\thechapter.}{1em}{\Huge}
\titlespacing*{\chapter}{0pt}{-19pt}{0pt}



\usepackage{booktabs,siunitx} 		% nicer spacing in tables, aligning
\sisetup{%								numbers in tables
	output-decimal-marker={.},		% 	based on commas (.)
}



